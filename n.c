#include <stdio.h>
#include <stdlib.h>

int main(){
    int count;
    int list[count];
    int gaessi = sizeof(list)/sizeof(int);

    scanf("%d",&count);
    for (int i = 0; i < count; i++)
    {
        for (int b = 0; b < i; b++)
        {
            
        }
        

    }
    printf("%d",gaessi);

    
}
